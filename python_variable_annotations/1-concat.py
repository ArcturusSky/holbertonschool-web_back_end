"""
Module to type_annotated a function concat to takes two string and return
a concatenated string.
"""


def concat(str1: str, str2: str) -> str:
    """
    Function that takes two strings and concatenate them
    """
    return str1 + str2
